# VR Controls

The tour uses the Touch controller ray and trigger to select world-space UI.
Hotspots change panorama, info buttons show the current location description,
and the Back to Menu control returns to the scene list.

This is a look-and-select experience: locomotion and gravity are intentionally
disabled so the user remains stationary while viewing each panorama.