# Panorama Asset Pipeline

Intranet locations use video panoramas. Custom Campus locations use the
equirectangular images in `Assets/CUSTOM IMG/Converted/`; the source DNG files
are retained for future reprocessing.

When replacing a panorama, preserve the equirectangular projection and check
the texture import settings on a Quest. A desktop preview does not reproduce
the headset's view or texture memory pressure.