# Android Build Notes

Use Unity 6000.4.5f1 with Android Build Support, then select Android in Build
Settings and keep the three tour scenes enabled. Development builds are useful
for headset validation because they preserve logs and profiler access.

If Gradle reports a PKIX certificate error, follow the certificate guidance in
the root README before changing project dependencies.